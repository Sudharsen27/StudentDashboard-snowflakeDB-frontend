# Optional. Omit to use same-origin /api (proxied to the backend via next.config rewrites).
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:5000

# Where Next.js proxies /api/* (server-side). Default: http://127.0.0.1:5000
# BACKEND_URL=http://127.0.0.1:5000

